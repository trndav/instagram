<div class="row">
  <div class="col-5" style="margin: auto; margin-top: 20px; margin-bottom: 10px; background-color: #f8f8f8; border-radius: 10px; padding: 5px; ">
   
    <h2>Edit <%= resource_name.to_s.humanize %></h2>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <div class="field" style="margin-bottom: 10px;>
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control" %>
      </div>
      <div class="field" style="margin-bottom: 10px;>
        <%= f.label :username %>
        <%= f.text_field :username, class:"form-control" %>
      </div>
      <div class="field" style="margin-bottom: 10px;>
        <%= f.label :bio %>
        <%= f.text_field :bio, class:"form-control" %>
      </div>

      <div class="field" style="margin-bottom: 10px;">
        <%= f.label :choose_avatar_image %>
        <%= f.file_field :avatar, class:"form-control" %>
      </div>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
      <% end %>      
      <div class="field" style="margin-bottom: 10px;">
        <%= f.label :set_new_password %><%= " " %><%= @minimum_password_length %> characters minimum <i>(leave empty if you don't want to change password)</i><br />
        <%= f.password_field :password, autocomplete: "new-password" , class:"form-control" %>
      </div>    
      <div class="field" style="margin-bottom: 10px;>
        <%= f.label :confirm_new_password %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password" , class:"form-control" %>
      </div>      
      <div class="field">
        <%= f.label :current_password %> <i>(password is required to confirm your changes)</i><br />
        <%= f.password_field :current_password, autocomplete: "current-password" , class:"form-control" %>
      </div>
      <div class="actions" style="margin-top: 10px;">
        <%= f.submit "Update" , class:"btn btn-success" %>
      </div>
    <% end %>
    </br>
    <h3>Cancel my account</h3>
    <div style="margin-top: 10px;">Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, class:"btn btn-danger" %></div>
  </div>
</div>
